import React from 'react'

const MovieReview = ({key, name, byline, headline, link, summary}) => {
    return (
        <div key = {key}>
            
        </div>
    )
}

export default MovieReview
